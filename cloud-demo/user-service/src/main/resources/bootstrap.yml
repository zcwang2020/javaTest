spring:
  application:
    name: user-service
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # nacos地址
        cluster-name: HZ
#        ip: 192.168.2.231
        port: 8082
    inetutils:
      preferred-networks: 192.168
      config:
        file-extension: yaml # 文件后缀名